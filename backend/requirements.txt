# PMS 后端依赖（2026-06 freeze，迁移到新机器时 pip install -r 即可）
# 注意：rebuild.sh 不会自动 pip install，重建 venv 后务必手动执行本文件。

Flask==3.1.3
Flask-Cors==6.0.2
Flask-SQLAlchemy==3.1.1

# Word 文档生成 / 合并（采购各类函、封面、归档一键打印）
python-docx==1.2.0
docxcompose==2.2.0

# Excel（评定标报告、用量导出等）
openpyxl==3.1.5

# 网页抓取（开标看板、四川采购公告）
beautifulsoup4==4.14.3
lxml==6.1.1
requests==2.34.2
playwright==1.60.0          # 还需 `playwright install chromium` 安装浏览器内核

# 图片处理（身份证照片、docx 内嵌图）
pillow==12.2.0

# 说明：SQLAlchemy / babel / soupsieve / urllib3 等为上述包的传递依赖，pip 会自动装。
# LLM 调用（投标审查/编制建议/AI 助手）走 requests 直连 API，无需 anthropic/openai SDK。
