# 四川政采数据治理 —— 当前进度交接（2026-06-16）

> 给网页终端新会话：在 `/home/huangxb/pms` 起 claude，记忆会自动载入
> [[ccgp-bulk-archive-12]]。本文件补充「正在进行、等用户拍板」的活状态。

## 这条线在干什么
四川政采全量库（卫健类）在 **.12（192.168.1.12）** 的 postgres `ccgp` 库 + `~/ccgp_clean`（68G 按项目附件）。目标：清洗成可用的医疗器械采购分析数据。

## 访问方式（已就绪）
- SSH 免密：`ssh 192.168.1.12`
- 连库：`ssh 192.168.1.12 'psql -w -h 127.0.0.1 -U ccgp -d ccgp ...'`（密码走 .12 的 `~/.pgpass`，**别在命令行写 PGPASSWORD，分类器会拦**）
- 进度脚本：`ssh 192.168.1.12 ~/ccgp_status.sh`
- 重下脚本：`.12:~/ccgp_redownload.py`（也在本仓库根）
- 核对文件：`.12:~/ccgp_verify/` 和本机 `/home/huangxb/ccgp_verify/`

## 已完成
1. 附件从 Hermes 散沙重下成按项目干净结构（89485 文件 /68G），旧 166G `ccgp_files` 已删。
2. 建 `proj_master`（PK=project_no 项目编号），合并同编号多 plan_id。
3. 删电子卖场/集采专区噪声 448 条+356 空壳文件夹（**保留 13 个"非集采/集采目录外"真项目**；删噪声只能用 `电子卖场|电子商场|集采专区`，**裸"集采"会误删真项目**）。
4. 现状：proj_master=43710 项目、notices=65382。

## ⏳ 当前悬而未决（等用户拍板）——金额异常处理
揪出 **413 条中标公告 amount<1000 的异常金额**（核对文件 `④_异常低金额_核对.txt`、`⑤_设备工程类金额抽错_可疑.txt`）。
**根因**：公告"中标（成交）金额"栏常为空，解析器误抓了**评审得分(40~100)**或**折扣率("单价折扣90%""下浮4.5")**；折扣率类项目本就没绝对金额。
- 175/413 可从 **items 单价合计** 捞回真实量级（例：食材"23元→实际4450万"、耗材"1.9元→2000万"）。
- 另 238 条 items 也无价，捞不回。

**我给的建议（未执行，等确认）**：不覆盖原 amount，改为加两字段：
- `amount_suspect boolean` —— 这 413 条标 true（金额存疑，可过滤）
- `amount_est numeric` —— = items 单价合计，给 175 条回填量级兜底
保持原值不丢、真值有兜底、存疑可过滤。

**用户最后在犹豫**：按上面加字段标记+回填，还是直接用 items 合计覆盖 / 只标记不回填。→ **新会话先问用户选哪种再动手。**

## 备选下一步（金额处理完之后）
① 按项目编号重组 `ccgp_clean` 文件夹；② 接 pms 前端做按项目编号检索；③ 对采购文件批量跑 .12 PaddleOCR 抽技术参数（[[bid-review-module]] 后续阶段）。
