# 采购项目管理系统 · 交接与重构蓝图（总纲）

> 内江市第一人民医院采购部内部系统
> 本文档是整个项目的"总纲"，用于在 **Claude Code / 本地 IDE** 中继续开发与重构。
> 现有代码已验证全部业务逻辑正确；本次重构只改"代码组织方式"，不改业务规则。

---

## 第一部分：项目现状

### 1.1 已建成的东西（跑在家里的 Ubuntu，B机）
| 系统 | 端口 | 说明 | 部署方式 |
|---|---|---|---|
| 本地大模型 | A机 192.168.1.10:8888 | Qwen3.6-35B，llama.cpp | start_model.bat |
| 采购开标看板（旧） | 8080 | 抓医院官网公告→14天开标+监督人 | systemd 自启 + cpolar(njyycgb.cpolar.top) |
| **采购项目管理系统** | 8081 | 本次主角，Flask 单体 | 手动运行（待做自启） |

- B机：Ubuntu，用户 huangxb，SSH：`ssh -p 14202 huangxb@6.tcp.cpolar.top`
- Python 虚拟环境：`~/test/venv`；项目管理系统目录：`~/pms/`
- 远程访问：cpolar，套餐含5个固定域名/2个TCP/2进程/12隧道每进程

### 1.2 项目管理系统已完成功能（10模块中的一部分）
- ✅ 登录 + 角色（助理/经办人/负责人/代理机构）
- ✅ 项目立项（自动编号、金额分流、招单价、单一来源走代理选择、草稿）
- ✅ 项目流程（列表、筛选[年度/方式/搜索]、进行中/已完成标签、编辑、删除）
- ✅ 开标管理（展示开标安排 + 代理/经办人标记能否开标）
- ✅ 权限隔离（经办人只看自己、代理只看自己、助理/负责人看全部）
- ✅ 按角色显示导航
- ✅ 修改密码

### 1.3 现有文件（~/pms/）
- `app.py`（单体，约900行，路由+HTML模板字符串+业务+SQL全混在一起）← 重构主要对象
- `numbering.py`（编号生成，逻辑已验证正确，可直接复用）
- `db_init.py`（建库+预置数据）
- `add_accounts.py`、`migrate_draft.py`、`migrate_bid.py`、`migrate_v4.py`（零散迁移脚本）
- `pms.db`（SQLite 数据库）

---

## 第二部分：业务规则（最宝贵资产，一字不可错）

### 2.1 三条采购流程线（按金额/是否走代理）
| 线 | 适用 | 采购方式 | 走代理 | 编号前缀 |
|---|---|---|---|---|
| A | <2万 | 院内议价 | 否 | NJYYXJ |
| B | 2万~<5万 | 院内询价 | 否 | NJYYXJ |
| C | ≥5万 | 院内竞选 | 是 | NJYYJX |
| C | 挂网耗材/招单价(无预算) | 院内竞选 | 是 | NJYYJX |
| A/B/C | 单一来源 | 院内单一来源 | 用户选 | 走→NJYYJX / 不走→NJYYXJ |

三条线终点都是"归档"。系统按金额自动推荐采购方式，仅允许手动改为"单一来源"。

### 2.2 编号规则（核心，已用代码验证）
- **不走代理**：`NJYYXJ-{YYMM}{序号3位}`
  - 序号 = 当月**全院所有不走代理项目**连续排号（议价/询价不分）
  - 例：26年5月第3个 → `NJYYXJ-2605003`
- **走代理**：`NJYYJX-{代理缩写}-{YYMM}{序号3位}`
  - 序号 = 当月**全院所有走代理项目**连续排号（与代理机构无关，缩写仅标记）
  - 例：5月全院第1个(华询)→`NJYYJX-HX-2605001`；第2个(尚璟)→`NJYYJX-SJ-2605002`；第3个(华询)→`NJYYJX-HX-2605003`
- 招单价等同≥5万竞选；单一来源按用户选的是否走代理决定用哪套。

### 2.3 代理机构缩写表
| 缩写 | 全称 |
|---|---|
| ZX | 四川知行招标代理有限公司 |
| ZZ | 内江中洲工程项目管理有限公司 |
| ZJ | 四川中锦招标代理有限公司 |
| SJ | 四川尚璟招标代理有限责任公司 |
| HX | 四川华询工程管理有限责任公司 |
| SY | 四川三盈招标代理有限公司 |
| CJ | 内江市川交公路勘察设计有限公司 |

### 2.4 角色与权限
| 角色(role) | 立项 | 数据范围 | 主要功能 |
|---|---|---|---|
| officer 经办人 | ✅ 仅本人 | 只看自己的项目(含草稿) | 立项、跟进、改状态、标记能否开标、做授权函 |
| assistant 助理 | ❌ | 看全部(只读) | 采购项目分发、归档（待开发） |
| leader 负责人 | ❌ | 看全部(只读) | 审核（待开发） |
| agency 代理机构 | ❌ | 只看自己代理的项目 | 编制文件、标记能否开标、拟合同 |
| supervisor 监督 | ❌ | （待定） | 开标监督、签字 |

- 经办人立项时经办人=本人，不可改。
- 代理机构账号绑定 agency_code 实现隔离。
- 将来"采购项目分发"模块由助理选经办人+代理机构，届时立项不再选代理（直接调用分发结果）。

### 2.5 账号规则
- 经办人(4人)：账号=姓名，密码=姓名拼音
  - 黄新博/huangxinbo、郑跃俊/zhengyuejun、谭群/tanqun、杨文炽/yangwenchi
- 代理机构(7家)：账号=公司全称，密码=大写缩写+123（如 ZJ123），登录后自改
- 测试账号：admin/admin123(助理)、leader/leader123(负责人)

### 2.6 监督人员 + 采购人代表（为授权函准备）
- 监督人员预置5位：刘堇羽、王勇、余兰、黄河、钟佩廷
- 采购人代表：有现成身份证照片，批量建库；缺的临时填并存库
- 两类人都需存：姓名、身份证号、身份证照片
- **授权函**：项目信息(名称含第几次/编号/开标时间/代理) + 监督人 + 采购人代表(姓名+身份证照片直接拼进) → 输出 Word + 可打印 HTML

---

## 第三部分：现有数据库结构（SQLite, pms.db）

```
projects（项目）
  id, number(编号), name(名称), amount(金额), line(A/B/C),
  method(采购方式), demand_dept(需求科室), manage_dept(归口科室),
  agency_code(代理缩写), officer(经办人), content(包号及内容),
  bid_time(开标时间), status(状态), supervisor(监督人), representative(采购人代表),
  created_by, created_at, updated_at, is_draft(草稿), can_open(能否开标),
  bid_note(开标备注), category(货物/服务/工程), year(采购年度)

users（用户）
  id, username, salt, pw_hash, role, display_name, active, agency_code

agencies（代理机构）
  id, code(缩写), name(全称), active

people（人员：监督人/采购人代表）
  id, name, id_card(身份证号), photo(照片路径), role_type(supervisor/representative/both), active

number_seq（编号序号计数）
  kind(XJ/JX), ym(年月如2605), seq(已用序号)  主键(kind,ym)
```

状态值 STATUSES：立项/委托代理/编制中/审核中/已发公告/报名中/开标/已定标/合同签订/已归档

---

## 第四部分：目标架构（已拍板）

### 4.1 技术选型（及取舍理由）
| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | React + TypeScript + Vite + Ant Design | 功能多、需长期维护，组件化更清晰；接受前端学习/调试成本 |
| 后端 | Flask，**纯 JSON API**（取消HTML模板） | 三层分层，彻底解决"代码混在一起" |
| 数据库 | **SQLite（暂不换 PG）** | 20人/年600项目，SQLite足够；零运维；将来不够再换 |
| 部署 | **不上 Docker**，直接运行 + systemd | 单机自用，8G内存还要跑大模型，省资源；将来迁服务器再容器化 |
| 版本管理 | **git** | 必须，改坏可回退 |

> 说明：砍掉 PG 和 Docker 是有意为之——降低复杂度、适配"一人+AI维护"。
> 将来要迁服务器：SQLite是单文件、无Docker依赖，迁移简单；届时再考虑容器化与PG。

### 4.2 后端分层规范（必须遵守）
```
1. 路由层 (routes/)    只接收参数、校验、返回 jsonify。不写业务、不写SQL。
2. 业务层 (services/)  核心逻辑：编号生成、流程流转、权限判断、授权函生成等。
3. 数据层 (models/)    SQLAlchemy ORM 模型 + 数据库读写。取代零散 migrate 脚本。
```

### 4.3 建议的项目目录结构
```
pms-system/
├── backend/                 # Flask 纯API
│   ├── app.py               # 入口，注册蓝图
│   ├── config.py
│   ├── models/              # SQLAlchemy 模型(projects/users/agencies/people)
│   ├── services/            # 业务逻辑(numbering复用现有逻辑/auth/project/bid/authletter)
│   ├── routes/              # API蓝图(auth_api/project_api/bid_api...)
│   ├── migrations/          # Alembic 数据库迁移(取代手写migrate脚本)
│   └── requirements.txt
├── frontend/                # React + Vite
│   ├── src/
│   │   ├── services/        # Axios API 封装(对应后端各API)
│   │   ├── components/      # 通用组件
│   │   ├── pages/           # 各功能页(项目立项/项目流程/开标管理...)
│   │   ├── hooks/           # 状态逻辑
│   │   └── App.tsx
│   └── package.json
└── README.md
```

### 4.4 配置动态注入（参考竞品的好做法）
后端首页返回时注入 `window.__APP_CONFIG__ = {api_base_url, app_name, ...}`，
前端启动直接读取，避免二次请求配置接口、便于将来换环境。
（此为前端脚手架细节，在 Claude Code 中实现。）

---

## 第五部分：重构迁移计划

### 5.1 原则
- 业务逻辑（第二部分）一字不改，只搬代码组织方式。
- `numbering.py` 的 decide()/gen_number() 逻辑已验证，直接搬进 services/numbering.py。
- 现有 pms.db 数据可保留：用 SQLAlchemy 映射现有表结构即可（表名/字段照第三部分）。

### 5.2 第一步重构（把已完成的2个功能转成API）
1. 用 SQLAlchemy 定义 4 张表的 ORM 模型（对照第三部分字段）。
2. 把 app.py 里的逻辑拆成：
   - services：编号、项目增删改查、权限过滤、开标标记
   - routes：登录、项目列表(带筛选/隔离)、立项、编辑、删除、开标管理、改密码 → 全部返回 JSON
3. 前端 React：先做登录页 + 项目流程列表页 + 立项表单页 + 开标管理页，调用上述API。
4. 验证：用现有业务规则逐条对照（编号、分流、权限隔离、进行中/已完成）。

### 5.3 后续开发顺序（剩余模块）
建议：① 授权函生成（最期待，要先做人员表导入身份证照片）→ ② 采购项目分发（助理选经办人+代理）→ ③ 采购文件编制(可接本地大模型辅助起草/查错) → ④ 挂网管理 → ⑤ 项目评审 → ⑥ 合同管理 → ⑦ 归档 → ⑧ 采购需求审签。
每次只聚焦一个小功能，控制上下文。

### 5.4 左侧导航完整结构（10模块）
1. 采购需求审签（采购需求编制/审签节点查看）
2. 采购项目分发（仅助理）
3. 代理协议
4. 采购项目管理（项目立项★/项目流程★/开标管理★/授权函）
5. 采购文件编制
6. 挂网管理（采购公告/调研公告/更正公告/单一来源公示/询议价函）
7. 项目评审（供应商报名/评定表报告/在线评审）
8. 采购结果确认
9. 合同管理
10. 归档（仅助理）
（★=已完成）

---

## 第六部分：安全与运维须知（重要）

- 系统将存储**身份证号+证件照片**等高敏感信息。正式给15-20人用前，部署与访问控制必须做好：
  - 不能长期裸跑在家用电脑+内网穿透；将来应迁至单位内网服务器或正规云。
  - 登录密码已用 PBKDF2 哈希；Flask secret_key 上线前必须改成随机串。
- 抓取医院官网是**过渡功能**：将来采购公告通过本系统发布，数据以立项为准，抓取会被淘汰。

---

## 第七部分：用 Claude Code 继续开发的启动指引

1. 在 B机（或开发机）装好：Node.js、Python venv、git；`git init` 建仓库。
2. 把现有 ~/pms/ 代码和 pms.db 拷入新仓库备份。
3. 打开 Claude Code，把本文档作为项目说明喂给它，告诉它：
   "按第四部分架构、第五部分计划，先把已完成的项目管理+开标管理两个功能重构成 React前端 + Flask纯API + SQLAlchemy，业务规则严格遵守第二部分。"
4. 每次只做一个小功能，做完用第二部分的规则验证，再 git commit。
5. 遇到前端(React/Vite/Ant Design)报错，把完整报错贴给 Claude Code 排查。

---

*本文档由开发过程整理而成，是项目的唯一总纲。后续所有开发以本文档的业务规则为准。*
